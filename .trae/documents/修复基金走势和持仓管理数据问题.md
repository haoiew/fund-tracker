## 问题分析

### 1. 基金走势无法显示
**根本原因**：request.ts 的响应拦截器已经自动解包了 `data.data`，但我们之前的修复又加了一层 `.data` 访问，导致双重解包，数据访问失败。

**修复方案**：
- 恢复 FundChart.vue 和 FundDetailDialog.vue 中的数据访问，直接使用 API 返回的数据，不要再访问 `.data`

### 2. 持仓管理数据不统一
**根本原因**：
- 表格中显示了两条相同的基金代码（012414），可能是数据重复
- 需要确保去重逻辑正确

**修复方案**：
- 检查 portfolioStore 中的数据计算逻辑
- 确保 itemCount 和表格显示的数据一致

## 涉及文件
1. `d:\workdir\code\Explore\fund-tracker-desktop\src\components\Charts\FundChart.vue`
2. `d:\workdir\code\Explore\fund-tracker-desktop\src\components\FundDetail\FundDetailDialog.vue`
3. `d:\workdir\code\Explore\fund-tracker-desktop\src\stores\dataManager.ts`

请确认后，我将执行修复。