## 问题诊断

### 1. 基金一直显示"获取中"的原因
截图中框选的基金都是 QDII/LOF 基金（如 007722 标普500、160125 南方香港优选等），这些基金在天天基金接口可能没有实时估值数据，需要依赖降级数据源。

当前问题：
- 前端没有正确显示降级后的数据源信息
- "获取中"状态没有及时更新为实际状态
- 需要确保后端返回正确的 status 字段

### 2. 操作按钮样式优化
截图中的操作按钮需要：
- 缩小尺寸（当前可能太大）
- 更紧凑的排列
- 更有设计感的图标样式

## 优化方案

### 1. 前端优化 - 完善表格布局和状态显示

修改 `frontend/src/pages/index/index.vue`：
- 改为完整的表格布局（匹配截图）
- 添加操作列（详情、添加、删除按钮）
- 优化状态显示逻辑：
  - 如果 estimate_nav 为 null 但 data_source 有值，显示对应数据源状态
  - 添加加载动画效果
  - 显示数据源标签

### 2. 前端优化 - 操作按钮样式

- 按钮尺寸：24px → 20px
- 使用圆形图标按钮
- 添加 hover 效果
- 更紧凑的间距

### 3. 后端优化 - 确保状态正确返回

检查 `fund_core_service.py` 确保：
- 当所有实时数据源失败时，正确返回 "最新净值" 状态
- status 字段正确传递

## 文件修改清单

1. `frontend/src/pages/index/index.vue` - 完整重构为表格布局
2. 检查 `backend/app/services/fund_core_service.py` - 确保状态正确

请确认后我将开始实施。