# 基金跟踪器项目优化计划

## 问题1: 路由与视图文件重复
- [ ] 删除 `views/HomeView.vue`（保留 `views/Home/HomeView.vue`）
- [ ] 将 `AboutView.vue` 添加到路由配置
- [ ] 清理未使用的视图组件

## 问题2: PortfolioItem 类型定义不一致
- [ ] 修改 `src/api/portfolio.ts` 中的字段名映射
- [ ] 更新 `src/stores/portfolioStore.ts` 使用正确的字段名
- [ ] 确保前端与后端字段名一致

## 问题3: API路径配置错误
- [ ] 检查后端 `app/api/v1/portfolio.py` 的实际路由路径
- [ ] 修正前端 `src/api/portfolio.ts` 中的 API 调用路径
- [ ] 测试接口连通性

## 问题4: 环境配置优化
- [ ] 在 `request.ts` 中添加环境变量支持
- [ ] 创建 `.env.development` 和 `.env.production` 文件
- [ ] 统一 API 地址管理

## 问题5: 清理未使用代码
- [ ] 删除 `src/stores/counter.ts`
- [ ] 清理 `reference/` 目录（如果确认不再需要）
- [ ] 检查并删除其他未使用的文件

## 问题6: 依赖版本检查
- [ ] 检查 `package.json` 中的依赖版本
- [ ] 更新已知有安全漏洞的包
- [ ] 运行类型检查确保无新问题

## 验证步骤
- [ ] 运行 `npm run type-check` 确保类型正确
- [ ] 运行 `npm run lint` 确保代码规范
- [ ] 测试核心功能页面加载