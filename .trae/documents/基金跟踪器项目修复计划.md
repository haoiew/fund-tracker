## 修复计划

### 阶段一：代码重构（高优先级）
1. **提取核心模块**
   - 创建 `fund_core.py` 包含 `FundTracker` 和 `PlotManager` 类
   - 修改 `fund.py`、`fund_main.py`、`fund_gui.py` 导入核心模块
   - 删除重复代码

### 阶段二：后端修复（高优先级）
2. **修复模型导入**
   - 更新 `models/__init__.py` 导出所有模型

3. **修复API路由**
   - 将同步服务方法改为异步，或使用 `run_in_executor`
   - 确保异步路由正确工作

4. **添加日志配置**
   - 创建 `app/logger.py` 统一日志配置
   - 替换所有 `print()` 为日志记录

5. **添加异常处理中间件**
   - 创建全局异常处理中间件
   - 统一API错误响应格式

### 阶段三：前端修复（中优先级）
6. **完善API请求封装**
   - 添加超时配置
   - 添加统一错误处理
   - 添加请求/响应拦截器
   - 支持环境变量配置 BASE_URL

### 阶段四：项目完善（中优先级）
7. **添加启动检查**
   - 检查数据库连接
   - 检查 Redis 连接（可选）
   - 检查必要依赖

8. **添加测试文件**
   - 创建 `tests/` 目录
   - 为核心功能编写单元测试

### 阶段五：代码质量（低优先级）
9. **完善类型注解**
10. **添加文档字符串**
11. **提取魔法数字为常量**

---

请确认此计划后，我将开始逐一修复这些问题。