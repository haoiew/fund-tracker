## 修复计划

### 问题1：基金筛选和持仓管理模块列表显示问题

**原因**：`el-table` 组件缺少高度设置，导致内容区域高度为0。

**修复方案**：
1. **ScreenView.vue** ([第36行](file:///d:/workdir/code/Explore/fund-tracker-desktop/src/views/Screen/ScreenView.vue#L36))：为 `el-table` 添加 `height` 属性，例如 `height="calc(100vh - 300px)"`
2. **PortfolioView.vue** ([第63行](file:///d:/workdir/code/Explore/fund-tracker-desktop/src/views/Portfolio/PortfolioView.vue#L63))：为 `el-table` 添加 `height` 属性，例如 `height="calc(100vh - 350px)"`

### 问题2：首页基金实时估值图表绘图失败

**原因**：`FundChart.vue` 中的图表初始化和数据加载逻辑存在问题，导致图表无法正常渲染。

**修复方案**：
1. **FundChart.vue** 优化 `loadData` 函数的执行顺序：
   - 确保 `initChart()` 在 `loadData` 之前正确完成
   - 确保 `chart.setOption()` 在 `loading` 状态正确管理后执行
   - 修复 `onMounted` 中的重复初始化问题

### 具体修改文件
1. `fund-tracker-desktop/src/views/Screen/ScreenView.vue` - 添加表格高度
2. `fund-tracker-desktop/src/views/Portfolio/PortfolioView.vue` - 添加表格高度
3. `fund-tracker-desktop/src/components/Charts/FundChart.vue` - 修复图表初始化逻辑