# 项目优化计划

## 一、代码重构（优先级：高）

### 1.1 消除代码重复
- 删除根目录下的重复文件（fund.py、fund_gui.py、fund_main.py）
- 将核心逻辑统一到 `fund_core.py`
- 删除 `reference/` 目录下的重复文件
- 保留 `fund-tracker/` 和 `fund-tracker-desktop/` 作为主要项目

### 1.2 统一配置管理
- 创建统一的配置文件结构
- 将 `fund_core.py` 中的 CONFIG 迁移到独立配置模块
- 环境变量统一管理
- 添加配置验证机制

## 二、代码质量提升（优先级：高）

### 2.1 改进错误处理
- 将所有空的 `except:` 块替换为具体的异常处理
- 添加错误日志记录
- 实现统一的异常处理中间件

### 2.2 统一日志管理
- 替换所有 `print()` 为 logger
- 配置日志级别和格式
- 添加日志轮转机制

### 2.3 添加类型注解
- 为所有函数添加完整的类型注解
- 使用 mypy 进行类型检查

## 三、项目结构优化（优先级：中）

### 3.1 添加 .gitignore
- 创建项目根目录的 .gitignore
- 配置 Python、Node.js、IDE 忽略规则

### 3.2 完善文档
- 创建项目根目录 README.md
- 说明三个项目的关系和使用场景
- 添加架构图和开发指南

### 3.3 优化目录结构
- 整理根目录文件
- 创建 scripts 目录存放工具脚本

## 四、功能完善（优先级：中）

### 4.1 完成未实现功能
- 实现基准指数数据获取（funds.py:155 TODO）
- 添加数据缓存机制
- 实现请求限流

### 4.2 添加测试
- 为核心模块添加单元测试
- 添加 API 集成测试
- 配置 CI/CD 测试流程

## 五、安全性加固（优先级：中）

### 5.1 配置安全
- 修改默认密钥为环境变量
- 限制 CORS 允许的域名
- 添加请求速率限制

### 5.2 依赖管理
- 更新依赖版本
- 添加依赖安全扫描
- 锁定依赖版本

## 六、性能优化（优先级：低）

### 6.1 数据缓存
- 实现基金数据缓存
- 添加 Redis 缓存支持
- 配置缓存过期策略

### 6.2 请求优化
- 优化批量请求处理
- 添加请求去重
- 实现并发控制

---

**预计工作量：** 4-6 小时
**风险等级：** 低（主要是重构和优化）