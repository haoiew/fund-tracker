## 问题分析

1. **增长率数据不显示**：`loadGrowthRates` 函数访问 API 返回数据时，没有正确处理嵌套的 `data` 字段
2. **历史走势图不显示**：`loadHistoryData` 函数同样存在数据结构访问问题
3. **FundChart.vue 组件也有同样问题**

## 修复内容

### 1. 修复 FundDetailDialog.vue
- 修改 `loadGrowthRates` 函数，正确访问 `response.data.values` 而不是 `response.values`
- 修改 `loadHistoryData` 函数，正确访问 `response.data.dates` 和 `response.data.values`

### 2. 修复 FundChart.vue
- 修改 `loadData` 函数，正确处理嵌套的 `data` 字段

## 涉及文件
1. `d:\workdir\code\Explore\fund-tracker-desktop\src\components\FundDetail\FundDetailDialog.vue`
2. `d:\workdir\code\Explore\fund-tracker-desktop\src\components\Charts\FundChart.vue`

请确认后，我将执行修复。